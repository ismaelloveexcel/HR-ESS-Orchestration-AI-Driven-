# Application Code (`/app`)

This directory contains proof-of-concept code generated by the **POC Agent**.

## Purpose

- Houses deployable application code
- Auto-deployed to Azure via `deploy-azure.yml` workflow
- Contains features built from issues and blueprints

## Current State

The main application code is currently in `/src`. This directory will be used for:
- New PoC implementations
- Feature prototypes
- Agent-generated code

## Related

- **Source Code**: See `/src` for the main HR ESS API
- **Blueprints**: See `/blueprints` for architecture designs
- **Deployment**: Managed by `deploy-azure.yml`
